[package]
name = "rv64-jit"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Phase-6 JIT for rv64.js: translates RV64 basic blocks into WebAssembly modules at runtime (v86-style)"
publish = false

[dependencies]
rv64-core = { path = "../rv64-core" }
