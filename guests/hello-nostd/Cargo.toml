[package]
name = "hello-nostd"
version = "0.1.0"
edition = "2021"

# Standalone (not part of the host workspace — different target).
[workspace]

[profile.release]
panic = "abort"
opt-level = "s"

[profile.dev]
panic = "abort"
