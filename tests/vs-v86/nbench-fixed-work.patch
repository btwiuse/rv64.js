--- a/nbench0.c
+++ b/nbench0.c
@@ -561,6 +561,7 @@
                 case PF_EMFLOOPS:       /* EMFLOOPS */
                         global_emfloatstruct.loops=
                                 (ulong)atol(eptr);
+                        global_emfloatstruct.adjust=1;
                         break;
 
                 case PF_EMFMINS:        /* EMFMINSECOND */
@@ -590,6 +591,7 @@
                 case PF_AARRAYS:        /* ASSIGNARRAYS */
                         global_assignstruct.numarrays=
                                 (ulong)atol(eptr);
+                        global_assignstruct.adjust=1;
                         break;
 
                 case PF_ASSIGNMINS:     /* ASSIGNMINSECONDS */
@@ -609,6 +611,7 @@
                 case PF_IDEALOOPS:      /* IDEALOOPS */
                         global_ideastruct.loops=
                                 (ulong)atol(eptr);
+                        global_ideastruct.adjust=1;
                         break;
 
                 case PF_IDEAMINS:       /* IDEAMINSECONDS */
@@ -764,6 +767,18 @@
 int i;                          /* Index */
 /* double newscore; */          /* For improving confidence interval */
 
+#ifdef SCORECARD_FIXED_WORK
+/* The scorecard measures one explicitly sized kernel invocation with the
+** host clock. Guest CPU clocks differ between emulators and must not choose
+** the amount of work or supply the cross-emulator performance value. */
+(*funcpointer[fid])();
+printf("\nSCORECARD_FIXED_WORK fid=%d\n",fid);
+*mean=(double)1.0;
+*stdev=(double)0.0;
+*numtries=1;
+return(0);
+#endif
+
 /*
 ** Get first 5 scores.  Then begin confidence testing.
 */
