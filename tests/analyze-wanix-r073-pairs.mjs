#!/usr/bin/env node

// The shared analyzer dispatches strictly from the immutable protocol schema;
// schema 3 is the candidate-specific R073 seven-by-three method.

await import("./analyze-wanix-r072-pairs.mjs");
